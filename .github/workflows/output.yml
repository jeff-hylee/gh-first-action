name: Workflow with output
on: [push, workflow_dispatch]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo "$RANDOM"
      
    
  second-job:
    needs: build
    runs-on: ubuntu-latest
    steps: 
      - name: post-build
        run: echo "post build step"
